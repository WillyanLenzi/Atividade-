real = float(input ("Digite: o valor monetário em R$: "))
taxa = float(input("Digite a cotaçaõ de conversão: "))
convertido = real / taxa
print ("O valor " + str(real) + "Convertido à taxa de " + str(taxa) + " é " + str(convertido))